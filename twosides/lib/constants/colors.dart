import 'package:flutter/material.dart';

// TODO: Set color palette by setup provider, fetch the color palette from DB

class TwoSidesColors {
  static const Color primaryColor = Color(0xFFF20707);
  static const Color secondaryColor = Color(0xFFB4E0FF);
  static const Color textColor = Color(0xFF000000);
  static const Color backgroundColor = Color(0xFFFFFFFF);
  //static const Color titleColor = Color();
  //static const Color linkColor = Color();
  //static const Color priceColor = Color();
}
